import React from "react";

import { Typography } from "@mui/material";

export const Home = () => {
  return (
    <Typography variant="h3" fontWeight="bold">
      Dashboard
    </Typography>
  );
};

export default Home;
